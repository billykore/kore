apiVersion: apps/v1
kind: Deployment
metadata:
  name: monorepo-book-app
  labels:
    name: monorepo-book-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: monorepo-book-service
  template:
    metadata:
      name: monorepo-book-service
      labels:
        name: monorepo-book-service
    spec:
      containers:
        - name: monorepo-book-app
          image: billykore/monorepo-book:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: monorepo-book-env
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 300m
              memory: 300Mi
      restartPolicy: Always

---

apiVersion: v1
kind: Service
metadata:
  name: monorepo-book-service
spec:
  selector:
    name: monorepo-book-service
  ports:
    - name: "http"
      port: 8000
      targetPort: 8000
    - name: "grpc"
      port: 9000
      targetPort: 9000
  type: ClusterIP
