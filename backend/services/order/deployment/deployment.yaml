apiVersion: apps/v1
kind: Deployment
metadata:
  name: monorepo-order-app
  labels:
    name: monorepo-order-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: monorepo-order-service
  template:
    metadata:
      name: monorepo-order-service
      labels:
        name: monorepo-order-service
    spec:
      containers:
        - name: monorepo-order-app
          image: billykore/monorepo-order:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: monorepo-order-env
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 300m
              memory: 300Mi
      restartPolicy: Always

---

apiVersion: v1
kind: Service
metadata:
  name: monorepo-order-service
spec:
  selector:
    name: monorepo-order-service
  ports:
    - name: "http"
      port: 8000
      targetPort: 8000
  type: ClusterIP
